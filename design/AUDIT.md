# Phase 0 — xSOM Signal

10 September 2026. Both repositories cloned from main into isolated task checkouts. Corporate site has 18 canonical FR/EN pages, a 404 and legacy redirect URLs; Vision was a redirect. Console has eight operational routes, with policy, tenants, credentials and DLP concentrated in administration.

Retain static GitHub Pages, CNAME, language URL structure, canonical/hreflang, contact integration, real published customer assets, local Saira/Inter/JetBrains Mono, original logo geometry, Next.js 14, auth and tenant/role contracts, FastAPI backend. Existing copper is shared but duplicated; backgrounds are navy, effects metallic, console uses a top-navigation layout.

Replace duplicated palette ownership with a portable generated-token package. Replace decorative full-time motion with inspectable mechanisms. Replace blank case-study templates with explicit available-on-request copy. Restore Vision as a paired editorial page. Add app sidebar, Inspector narrative, expiry countdown, inspectable audit, dedicated policy/risk/settings routes and honest missing-data states.

Existing API exposes approval timestamps, tool registry, policy, trust and integrity. Audit list does not expose chain hashes; no cryptographic verification claim will be invented. Existing backend and control proxy behavior stay authoritative. Demonstration flows never call live action endpoints.

Delivery sequence: shared source and component preview; corporate FR/EN; operational console; interaction/visual and contract tests; atomic commits and two reviewed PRs; merge and verify actual published SHA/assets. Website remains build-free at deploy time. `design-spec.md` records the chosen identity.
